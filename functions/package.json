{
  "name": "glaceon-masterset-functions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "src/index.js",
  "scripts": {
    "lint": "oxlint --deny-warnings src",
    "test": "vitest run",
    "build": "echo 'No build step required for ESM functions'",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.2.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "oxlint": "^1.79.0",
    "vitest": "^3.0.4"
  }
}
